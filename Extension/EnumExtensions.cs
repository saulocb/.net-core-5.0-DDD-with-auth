using System;

namespace Extensions
{
    public static class EnumExtensions 
    {
        public static bool Equals(this Enum @enum, Enum other)
        {
            throw new NotImplementedException();
        }
    }
}
